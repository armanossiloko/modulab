# GENERATED from catalog/*/recipe.json — do not edit by hand.
# Regenerate: bash scripts/render-config.sh
# LAB_HOST_IP / PIHOLE_LOCAL_DOMAIN come from generated .env.
# Merged when starting Pi-hole (see scripts/common.sh).
services:
  pihole:
    environment:
      FTLCONF_dns_hosts: |-
        ${LAB_HOST_IP} bentopdf.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} home.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} notes.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} futo-notes.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} immich.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} it-tools.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} jellyfin.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} n8n.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} picoshare.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} pihole.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} postgres.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} searxng.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} seerr.${PIHOLE_LOCAL_DOMAIN}
        ${LAB_HOST_IP} stirling.${PIHOLE_LOCAL_DOMAIN}
