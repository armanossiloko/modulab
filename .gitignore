# OS and editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/

# Dependencies
node_modules/

# VS Code launch and tasks are tracked intentionally
# .vscode/

# Runtime / bind-mounted data (Docker stacks)
data/
media/
.data/

# .NET build outputs
**/bin/
**/obj/
control-center/src-backend/**/bin/
control-center/src-backend/**/obj/
src-backend/**/bin/
src-backend/**/obj/

# Secrets and local env (never commit passwords or generated keys)
secrets/
lab.config.json
.env
.env.*
!.env.*.example
!.env.example

# Optional per-machine Compose overrides (Linux: docker compose merges these automatically)
docker-compose.override.yml
docker-compose.*.override.yml

# Logs and temp files
*.log
*.tmp

# Generated Control Center UI + runtime configs (seeded by scripts/ensure-wwwroot.sh)
control-center/wwwroot/
control-center/ui/dist/
