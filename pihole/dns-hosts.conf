# Local DNS host labels — must stay in sync with:
#   FTLCONF_dns_hosts in docker-compose.pihole.yml (subdomains only)
#   site blocks in caddy/Caddyfile (optional network.lan proxy)
#   home/services.json (dashboard ports — works without Pi-hole/Caddy)
# FQDN: <label>.<PIHOLE_LOCAL_DOMAIN>  ·  IP: LAB_HOST_IP in .env.pihole

jellyfin
n8n
seerr
it-tools
stirling
immich
odysseus
searxng
ntfy
postgres
pihole
