# Local DNS host labels — must stay in sync with:
#   FTLCONF_dns_hosts in docker-compose.pihole.yml
#   site blocks in caddy/Caddyfile (web UIs only; postgres is DNS/TCP-only)
# FQDN: <label>.<PIHOLE_LOCAL_DOMAIN>  ·  IP: LAB_HOST_IP in .env.pihole

jellyfin
n8n
seerr
it-tools
stirling
immich
odysseus
searxng
ntfy
postgres
pihole
