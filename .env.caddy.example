# cp .env.caddy.example .env.caddy
# Must match PIHOLE_LOCAL_DOMAIN in .env.pihole (same private zone as Pi-hole DNS).

PIHOLE_LOCAL_DOMAIN=network.lan

# Pin a release tag — https://hub.docker.com/_/caddy/tags
CADDY_TAG=2-alpine
