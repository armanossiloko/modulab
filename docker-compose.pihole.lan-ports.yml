# Merged when ENABLE_LAN_PROXY=true (see scripts/common.sh).
# Bind DNS to the lab host LAN IP so systemd-resolved on 127.0.0.53 stays free.

services:
  pihole:
    ports:
      - "${MODULAB_HOST_IP}:53:53/tcp"
      - "${MODULAB_HOST_IP}:53:53/udp"
